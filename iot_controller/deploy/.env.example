# Copy to .env and adjust values.
# Generate passwords (example):
#   openssl rand -base64 24
# Credentials live in iot_controller.env (MONGO_URI/RABBITMQ_URL) and gitops/.env.
IMAGE_TAG=local
IOT_CONTROLLER_PORT=8000
