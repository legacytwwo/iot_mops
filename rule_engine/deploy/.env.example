# Copy to .env and adjust values.
# Generate passwords (example):
#   openssl rand -base64 24
# Credentials live in rule_engine.env (MONGO_URI/RABBIT_URL) and gitops/.env.
IMAGE_TAG=local
RULE_ENGINE_PORT=8080
